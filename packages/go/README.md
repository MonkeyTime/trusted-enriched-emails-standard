# Realtime Mail Go SDK

Go SDK for validators, trust policy, canonicalization, and Ed25519 signature verification.

This package intentionally uses the Go standard library only.

```bash
go test ./...
```

From the repository root:

```bash
npm.cmd run go:check
```
